# VeriFlow
We are tackling the Agentic Workflows track by building a zero-trust, self-healing data extraction pipeline. Instead of relying on naive LLM outputs, we are wrapping Gemini in strict deterministic evidence gates to catch, evaluate, and autonomously correct hallucinations in real-time.
